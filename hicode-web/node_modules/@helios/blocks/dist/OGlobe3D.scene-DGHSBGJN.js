import { Canvas, useFrame } from '@react-three/fiber';
import { OrbitControls, Sphere } from '@react-three/drei';
import { useState, useMemo, useRef } from 'react';
import * as THREE from 'three';
import { jsxs, jsx } from 'react/jsx-runtime';

var GLOBE_RADIUS = 1;
function latLngToVec3(lat, lng, r = GLOBE_RADIUS) {
  const phi = (90 - lat) * (Math.PI / 180);
  const theta = (lng + 180) * (Math.PI / 180);
  return new THREE.Vector3(
    -(r * Math.sin(phi) * Math.cos(theta)),
    r * Math.cos(phi),
    r * Math.sin(phi) * Math.sin(theta)
  );
}
function Marker({
  position,
  value,
  label,
  color,
  onHover
}) {
  const ref = useRef(null);
  const size = 0.012 + Math.min(value / 100, 1) * 0.035;
  useFrame((state) => {
    if (!ref.current) return;
    const t = state.clock.elapsedTime;
    const scale = 1 + Math.sin(t * 2 + position.x) * 0.15;
    ref.current.scale.set(scale, scale, scale);
  });
  return /* @__PURE__ */ jsxs(
    "mesh",
    {
      ref,
      position,
      onPointerOver: (e) => {
        e.stopPropagation();
        onHover(label);
      },
      onPointerOut: () => onHover(null),
      children: [
        /* @__PURE__ */ jsx("sphereGeometry", { args: [size, 12, 12] }),
        /* @__PURE__ */ jsx("meshBasicMaterial", { color, transparent: true, opacity: 0.9 })
      ]
    }
  );
}
function Arc({
  from,
  to,
  color
}) {
  const curve = useMemo(() => {
    const mid = from.clone().lerp(to, 0.5).normalize().multiplyScalar(GLOBE_RADIUS * 1.4);
    return new THREE.QuadraticBezierCurve3(from, mid, to);
  }, [from, to]);
  const points = useMemo(() => curve.getPoints(48), [curve]);
  const geometry = useMemo(() => new THREE.BufferGeometry().setFromPoints(points), [points]);
  return /* @__PURE__ */ jsxs("line", { children: [
    /* @__PURE__ */ jsx("primitive", { object: geometry, attach: "geometry" }),
    /* @__PURE__ */ jsx("lineBasicMaterial", { color, transparent: true, opacity: 0.4 })
  ] });
}
function GlobeMesh({ autoRotate, globeColor }) {
  const ref = useRef(null);
  useFrame((_, delta) => {
    if (autoRotate && ref.current) {
      ref.current.rotation.y += delta * 0.08;
    }
  });
  return /* @__PURE__ */ jsx(Sphere, { ref, args: [GLOBE_RADIUS, 48, 48], children: /* @__PURE__ */ jsx(
    "meshBasicMaterial",
    {
      color: globeColor,
      wireframe: true,
      wireframeLinewidth: 1,
      transparent: true,
      opacity: 0.35
    }
  ) });
}
function GlobeScene({ data, autoRotate, interactive, markerColor, arcColor, globeColor }) {
  const [hoverLabel, setHoverLabel] = useState(null);
  const markers = useMemo(
    () => data.markers.map((m, i) => ({
      ...m,
      key: `m-${i}`,
      position: latLngToVec3(m.lat, m.lng, GLOBE_RADIUS * 1.02)
    })),
    [data.markers]
  );
  const arcs = useMemo(
    () => (data.arcs ?? []).map((a, i) => ({
      key: `a-${i}`,
      from: latLngToVec3(a.from[0], a.from[1]),
      to: latLngToVec3(a.to[0], a.to[1]),
      value: a.value
    })),
    [data.arcs]
  );
  return /* @__PURE__ */ jsxs("div", { className: "relative w-full h-full @container", children: [
    /* @__PURE__ */ jsxs(Canvas, { camera: { position: [0, 0, 3], fov: 45 }, dpr: [1, 2], children: [
      /* @__PURE__ */ jsx("ambientLight", { intensity: 0.6 }),
      /* @__PURE__ */ jsx("pointLight", { position: [5, 5, 5], intensity: 0.8 }),
      /* @__PURE__ */ jsx(GlobeMesh, { autoRotate, globeColor }),
      /* @__PURE__ */ jsx("group", { children: markers.map((m) => /* @__PURE__ */ jsx(
        Marker,
        {
          position: m.position,
          value: m.value,
          label: m.label,
          color: markerColor,
          onHover: setHoverLabel
        },
        m.key
      )) }),
      /* @__PURE__ */ jsx("group", { children: arcs.map((a) => /* @__PURE__ */ jsx(Arc, { from: a.from, to: a.to, color: arcColor }, a.key)) }),
      interactive ? /* @__PURE__ */ jsx(
        OrbitControls,
        {
          enablePan: false,
          enableZoom: true,
          autoRotate: false,
          minDistance: 1.8,
          maxDistance: 5
        }
      ) : null
    ] }),
    hoverLabel ? /* @__PURE__ */ jsx("div", { className: "absolute top-3 left-3 px-3 py-1.5 bg-popover/90 text-popover-foreground text-sm rounded-md border border-border backdrop-blur-sm pointer-events-none", children: hoverLabel }) : null
  ] });
}
var OGlobe3D_scene_default = GlobeScene;

export { GlobeScene, OGlobe3D_scene_default as default };
//# sourceMappingURL=OGlobe3D.scene-DGHSBGJN.js.map
//# sourceMappingURL=OGlobe3D.scene-DGHSBGJN.js.map