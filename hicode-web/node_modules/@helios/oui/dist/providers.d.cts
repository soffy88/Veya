import * as react from 'react';
import { ReactNode } from 'react';
import { UiLang } from '@helios/blocks';

interface OAppProvidersProps {
    children: ReactNode;
    /** 主题名(如 'professional' / 'terminal-dark' / 'mneme-friendly')。 */
    theme?: string;
    /** 颜色模式。'system' 跟随操作系统明暗(blocks 大于等于 2.11)。 */
    mode?: 'light' | 'dark' | 'system';
    /** UI 密度。 */
    density?: 'compact' | 'comfortable' | 'terminal';
    /** 界面语言。默认 'zh-en'(中英并列);'zh' 纯中文;'en' 纯英文。 */
    lang?: UiLang;
}
declare function OAppProviders({ children, theme, mode, density, lang, }: OAppProvidersProps): react.JSX.Element;

export { OAppProviders, type OAppProvidersProps };
