[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "veya"
version = "0.2.0"
description = "veya — AI coding agent (복제 + opencode/Claude Code 초월)"
requires-python = ">=3.11"
dependencies = ["pygments>=2.0", "anthropic>=0.20.0"]

[project.scripts]
veya = "veya_tui.repl:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["veya_tui*"]
